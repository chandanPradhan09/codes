#include <stdio.h>

int main()
{
    int no;
while (1)
{
     printf("Menu");
     printf("1. Creation of Linklist\n 2. counting the no of linked list");
    printf("Enter your choices: ");
    scanf("%d",&no);
    switch (no)
    {
    case 1:
        /* code */
        break;
    
    default:
        break;
    }
}

    return 0;
}